任务二：

1、修改了Term的代码，实现了comparable接口的compareTo()方法，修改原因为用来比较Term的大小，方便调用Collections.sort()方法进行排序；

2、修改了Term的代码，增加了getIndex()和getCoef()方法，修改原因为方便外界访问其私有属性；

3、修改了Input的代码，增加了Collections.sort，修改原因为对Term按照大小进行排序；

4、修改了output的代码，修改原因为增加了特殊情况的判定，对于前七条规则分别按照规则输出；

5、修改了toString的代码，将“+”删去，修改原因是不要求输出正整数符号。

